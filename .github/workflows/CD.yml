name: CD workflow to NPM publish

on:
  release:
    types: [published]

jobs:
  publish-release:
    if: "!github.event.release.prerelease"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 14
      - run: git config --global user.name "JeremyDolle"
      - run: git config --global user.email "jeremy.dolle34@gmail.com"
      - run: npm version ${{ github.event.release.tag_name }}
      - run: git commit -am "🏷️(version) Update boilerplate to version ${{ github.event.release.tag_name }}"
      - run: git push
        env:
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{ secrets.NPM_TOKEN }}
  publish-pre-release:
    if: "github.event.release.prerelease"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 14
      - run: git config --global user.name "JeremyDolle"
      - run: git config --global user.email "jeremy.dolle34@gmail.com"
      - run: npm version ${{ github.event.release.tag_name }}
      - run: git commit -am "🏷️(version) Update boilerplate to version ${{ github.event.release.tag_name }}"
      - run: git push
        env:
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{ secrets.NPM_TOKEN }}
          tag: ${{ github.event.release.tag_name }}
