export const fetchUser = async (userId, thunkAPI) => {
  // console.log('hello', thunkAPI.getState())
  // const response = await userAPI.fetchById(userId)
  // console.log(response)
  return 'TOTO'
}
